export const userRegister = state => {
    return state.userReduser.userRegister
}

export const userMessage = state => {
    return state.userReduser.userMessage
}

export const userId = state => {
    return state.userReduser.userInfo.userId
}
